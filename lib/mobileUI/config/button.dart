import 'package:flutter/material.dart';

class ButtonConfig {
  /// 按钮背景色
  static const Color backgroundColor = Color.fromRGBO(62, 103, 255, 1);

  /// 按钮文本颜色
  static const Color textColor = Colors.white;

  /// 按钮按下时颜色
  static const Color highlightColor = Color.fromRGBO(62, 103, 255, 1);

  /// 按钮水波纹颜色
  static const Color splashColor = Color.fromRGBO(62, 88, 88, 1);

  /// 按钮禁用颜色
  static const Color disabledColor = Color.fromRGBO(62, 103, 255, 0.6);

  ///按钮禁用 文本色
  static const Color disabledTextColor = Colors.white;

  /// 按钮高度
  static const double height = 45.0;

  /// 按钮线框宽度
  static const double borderWidth = 1.0;

  /// 按钮线框颜色
  static const Color borderColor = Colors.transparent;

  /// 投影
  static const double elevation = 1.0;

  /// 圆角
  static const double radius = 8.0;

  /// 字体大小
  static const double fontSize = 12.0;
}
